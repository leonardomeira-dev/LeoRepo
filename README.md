# Calculadora

Calculadora simples em Python com as operações:

- soma (`+`)
- subtração (`-`)
- multiplicação (`*`)
- divisão (`/`)

## Como executar

```bash
python3 calculadora.py
```

A aplicação é interativa e permite realizar várias contas em sequência.
